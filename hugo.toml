baseURL = "https://feolp-19.github.io/passive-server/"
languageCode = "en-us"
title = "Passive Income Lab - Earn Money From Your Idle Computer"
theme = "PaperMod"

# SEO & Performance
enableRobotsTXT = true
buildDrafts = false
buildFuture = false
buildExpired = false
enableEmoji = true
canonifyURLs = true
enableGitInfo = true

# Better URLs for SEO (clean URLs without /posts/)
[permalinks]
  posts = "/:slug/"

# Minify for faster loading (SEO ranking factor)
[minify]
  disableXML = true
  minifyOutput = true

# Sitemap settings
[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5

[params]
  env = "production"
  title = "Passive Income Lab"
  description = "Learn how to earn $40+/month passive income with bandwidth sharing apps like Grass, Honeygain, EarnFM, Traffmonetizer and storage rental with Storj. Step-by-step guides for beginners."
  keywords = ["passive income", "bandwidth sharing", "Grass app", "Honeygain review", "EarnFM", "Traffmonetizer", "Storj node", "make money online", "side hustle 2026", "earn money from old computer", "passive income apps", "bandwidth monetization"]
  author = "Passive Income Lab"
  
  # Open Graph / Social Media SEO
  images = ["/images/og-image.png"]
  
  # Schema.org structured data
  [params.schema]
    type = "Blog"
    name = "Passive Income Lab"
    description = "Guides for earning passive income with bandwidth sharing and storage rental apps"
  
  # SEO
  defaultTheme = "auto"
  ShowReadingTime = true
  ShowShareButtons = true
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = true
  ShowWordCount = false
  ShowRssButtonInSectionTermList = true
  UseHugoToc = true
  disableSpecial1stPost = false
  disableScrollToTop = false
  comments = false
  hidemeta = false
  hideSummary = false
  showtoc = true
  tocopen = false

  # Home page
  [params.homeInfoParams]
    Title = "🖥️ My Mac Mini Earns $40+/Month While I Sleep"
    Content = """
I turned an old Mac Mini running Linux into a **passive income machine**.

**My current earnings:**
- 💚 Grass → ~$15/month
- 🍯 Honeygain → ~$10/month  
- 🎵 EarnFM → ~$5/month
- 🚗 Traffmonetizer → ~$4/month
- ☁️ Storj (4TB) → ~$8/month

**Total: ~$42/month** from hardware collecting dust!

👇 **Pick an app below and start earning today** 👇
"""

  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/feolp-19"
  
  [[params.socialIcons]]
    name = "rss"
    url = "/index.xml"

  # For SEO
  [params.cover]
    hidden = false
    hiddenInList = false
    hiddenInSingle = false

  [params.fuseOpts]
    isCaseSensitive = false
    shouldSort = true
    location = 0
    distance = 1000
    threshold = 0.4
    minMatchCharLength = 0
    keys = ["title", "permalink", "summary", "content"]

  # Profile mode (alternative to homeInfoParams)
  [params.profileMode]
    enabled = false

# Main menu
[menu]
  [[menu.main]]
    identifier = "apps"
    name = "💸 Apps"
    url = "/tags/apps/"
    weight = 10
  [[menu.main]]
    identifier = "guides"
    name = "📚 Guides"
    url = "/tags/guide/"
    weight = 20
  [[menu.main]]
    identifier = "about"
    name = "ℹ️ About"
    url = "/about/"
    weight = 30

# Outputs for SEO
[outputs]
  home = ["HTML", "RSS", "JSON"]

# Taxonomies
[taxonomies]
  category = "categories"
  tag = "tags"
  series = "series"

# Markup settings
[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = false
    noClasses = false
    style = "monokai"
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
